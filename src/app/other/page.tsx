import ContentContainer from "../main-layout/content-container";
import MainLayout from "../main-layout/main-layout";

export default function otherList() {
    return <MainLayout>
        <ContentContainer>
            기타-홈 페이지가 Figma에서 구현되어있지 않아 구현 안함.
            공지는 /other/notice
            어워즈는 /other/awards
            축전은 /other/celebrate
            로 접속해주세요
        </ContentContainer>
    </MainLayout>
}