import { TopNavbarItem } from "./top-navbar"

type Prop = {
    items: TopNavbarItem[];
    className?: string;
}

export default function MobileNavbar({ items, className }: Prop) {
    return <div></div>
}